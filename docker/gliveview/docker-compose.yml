services:
  gliveview:
    build:
      context: .
      args:
        - AMARU_NETWORK
    volumes:
      - gliveview-blocklog:/opt/cardano/blocklog
    stdin_open: true
    tty: true
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  gliveview-blocklog:
