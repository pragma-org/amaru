[project]
name = "haskell-node-extractor-tools"
version = "0.0.0"
description = "Python utilities for the Haskell node extractor"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "jsonschema>=4.17,<5",
]

[tool.uv]
package = false
