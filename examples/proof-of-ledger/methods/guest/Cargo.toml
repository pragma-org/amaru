[package]
name = "guest"
version = "0.1.0"
edition = "2021"

publish = false

[workspace]

[dependencies]
risc0-zkvm = { version = "1.2.4", default-features = false, features = ['std'] }
amaru-examples-shared = { path = "../../../shared" }